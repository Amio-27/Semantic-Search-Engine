# Preferred in production: keep backend URL server-side and use /api proxy.
API_BASE_URL=https://your-backend-domain.onrender.com

# Optional: only set if you explicitly want direct browser-to-backend requests.
NEXT_PUBLIC_API_BASE_URL=
